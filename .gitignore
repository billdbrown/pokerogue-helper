# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
venv/

# PyInstaller build output
build/
dist/

# Embedded browser profile (cookies, cache, local storage) — per-user
browser_data/

# Runtime caches (per-user, not in version control for packaged builds)
# In development the app writes to data/ which IS tracked; nothing to ignore here.
moves_cache.json
tier_cache.json

# Per-user UI state and OCR tuning
box_positions.json
window_state.json

# Logs and debug artifacts
*.log
embedded_console.log
debug/

# IDE / editor
.vscode/
.idea/
.claude/
*.swp
*~

# OS
.DS_Store
Thumbs.db
